<nav class="pagination" role="navigation">
    <div id="newer-posts">
	    {{#if prev}}
	        <a href="{{page_url prev}}">&larr; Newer Posts</a>
	    {{/if}}
    </div>
    <span id="page-number">Page {{page}} of {{pages}}</span>
    <div id="older-posts">
	    {{#if next}}
	        <a href="{{page_url next}}">Older Posts &rarr;</a>
	    {{/if}}
    </div>
</nav>